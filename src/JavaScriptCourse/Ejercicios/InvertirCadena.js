let text = "Hello World";
console.log("Normal String");
console.log(text);

//To reverse the string, we need to use a for loop
for(let i = text.length - 1; i >= 0; i--) {

    console.log(text[i]);
}
