let wrestler = []
let company = []

//
function resgister(object) {
    //Validar si vamos a guardar a un luchador o a una compañia
    if(Object.keys(object).length > 4){
        //
        for(let i = 0; i <wrestler.length; i++){
            if(wrestler[i].idWrestler === object.idWrestler){


            }
        }
    }
}